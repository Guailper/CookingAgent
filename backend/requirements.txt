# Web framework and ASGI server
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# Database and migrations
sqlalchemy>=2.0,<3.0
pymysql>=1.1,<2.0
alembic>=1.13,<2.0

# Configuration and validation
pydantic>=2.8,<3.0
python-dotenv>=1.0,<2.0

# LangChain agent runtime and OpenAI-compatible chat model adapter
langchain>=1.0,<2.0
langchain-core>=1.0,<2.0
langchain-openai>=1.0,<2.0

# RAG vector storage and HTTP rerank adapter
pymilvus>=2.4,<3.0
httpx>=0.27,<1.0
sentence-transformers>=3.0,<4.0
FlagEmbedding>=1.2,<2.0

# Multipart upload parsing
python-multipart>=0.0.9,<1.0

# Local speech-to-text fallback/replacement for external voice APIs
faster-whisper>=1.1,<2.0

# Optional Redis cache, rate limit, and short-lived verification code storage
redis>=5.0,<6.0
